# 递推 vs 递归 · 课堂包（Python & C++）

配套：`easy.html` 交互动画 + VOX 白板片。

---

## 1. 同一题：爬楼梯

到第 n 级台阶的走法：每次可跨 1 或 2 级。

```
f(n) = f(n-1) + f(n-2)
f(1)=1, f(2)=1
f(6)=8
```

---

## 2. 递归（问下去再答）

**思想**：要求 f(n)，先相信 f(n-1)、f(n-2) 已算好，问出来相加。

### C++

```cpp
long long f(int n) {
    if (n <= 2) return 1;
    return f(n - 1) + f(n - 2);
}
```

### Python

```python
def f(n):
    if n <= 2:
        return 1
    return f(n-1) + f(n-2)
```

**风险**：重复计算 f(3) 很多次；n 大时爆栈（Python 默认深度约 1000）。

---

## 3. 递推（从小到大填表）

**思想**：先记边界，再用前两项推出当前项，填到 n。

### C++

```cpp
int dp[10];
dp[1] = 1; dp[2] = 1;
for (int i = 3; i <= n; i++)
    dp[i] = dp[i-1] + dp[i-2];
cout << dp[n];
```

### Python

```python
n = 6
dp = [0] * (n + 1)
dp[1] = dp[2] = 1
for i in range(3, n + 1):
    dp[i] = dp[i-1] + dp[i-2]
print(dp[n])
```

**滚动优化（Python）**：

```python
a, b = 1, 1
for _ in range(n - 2):
    a, b = b, a + b
print(b)
```

---

## 4. 对比表

| | 递归 | 递推 |
|---|---|---|
| 方向 | 大 → 小问，再归 | 小 → 大填 |
| 边界 | if 出口 | dp[1] dp[2] |
| 核心 | f(n) 调自己 | dp[i] 用前两项 |
| 风险 | 重复算、爆栈 | 开数组、想清顺序 |
| 适合 | 树形/分治/汉诺塔 | 爬楼梯/斐波那契/前缀和 |

---

## 5. 题单

1. 用递归和递推各写一遍 f(10)，核对结果相同。  
2. 递归版打印 f(3) 被调用次数，体会重复计算。  
3. 递推滚动版：只用两个变量求 f(20)。  
4. 斐波那契：1,1,2,3,5… 用递推输出前 15 项。  
5. 爬楼梯变体：一次可跨 1/2/3 级，写出递推式。  
6. 对比：把递归版改成带记忆化的递归（memo），再和递推比。

### 思考题

7. 为什么说「能递推的往往也能递归，能递归的不一定好写递推」？  
8. 汉诺塔适合用递推吗？为什么？

---

## 6. 课堂流程

1. easy.html ① 递归栈动画（8′）  
2. ② 递推填表（8′）  
3. ③ 双语言代码对照（10′）  
4. 黑板默写两种写法（8′）  
5. 当堂题 1、2（10′）  
6. ④ 对比总结（5′）  
