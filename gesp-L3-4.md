# GESP 三～四级 · 考法讲义（可打印）

> 依据 CCF 大纲 + 2026-09 等真题归纳。不转载整卷。  
> 官网真题：https://gesp.ccf.org.cn/101/1010/index.html

---

## 三级必会

| 知识点 | 常见问法 | 出题思路 | 做题方法 |
|---|---|---|---|
| 进制转换 | 十进制↔二进制结果 | 短除/按权 | 纸笔列余数或权重 |
| **位运算** | `& \| ^ << >>` 结果 | 当「灯」不当十进制 | 写 8 位 0/1 再算 |
| 一维数组/列表 | 下标读写、越界 | 从 0 数 | 画格子标下标 |
| 字符串 | 遍历、切片、回文 | 逐字符 | 把字符串摊开写 |
| 枚举/模拟 | 按题意暴力跑 | 不会错但要细心 | 先写步骤再写码 |
| 逻辑运算 | and/or/not 真值 | 复合条件 | 列真值小表 |

**2026-09 线索**：C++ 三级选择里位运算、数组下标、循环边界仍高频。

---

## 四级必会

| 知识点 | 常见问法 | 出题思路 | 做题方法 |
|---|---|---|---|
| **函数** | 参数默认值、返回类型 | 调用是否合法 | 对照签名画调用 |
| **递归** | 阶乘/斐波那契输出 | 出口+递推式 | 画套娃/返回值树 |
| **结构体（C++）** | 定义、点号、数组 | 打包字段 | 档案袋卡片 |
| **类（Py）** | 属性方法、self | 哪个对象 | 画对象卡片 |
| 文件 / 异常 | 打开模式、try-except | 边界与失败 | 走异常分支 |
| 浅拷贝陷阱 | `list.copy` 后改内层 | 引用同一对象 | 画「同一房间两个门牌」 |

**2026-09 线索**：Py 四级大量「阅读程序写结果」（copy/推导式/lambda）；C++ 四级递归与指针/引用并重。

---

## 展示台对照（点开就能练）

| 三级 | 四级 |
|---|---|
| `platform.html?lesson=cpp3-8` 位运算 | `platform.html?lesson=py4-1` 递归套娃 |
| `platform.html?lesson=py3-6` 进制 | `platform.html?lesson=py4-2` 兔子 |
| `platform.html?lesson=cpp3-1` 数组 | `platform.html?lesson=cpp4-6` 结构体 |
| `platform.html?lesson=py3-5` 回文 | `platform.html?lesson=cpp4-2` 指针 |

---

## 课后 8 题（自测）

1. `13 & 5`、`13 | 5`、`13 ^ 5` 各是多少？（先写二进制）  
2. 把 25 转成二进制，再按权验回。  
3. `a=[1,2,3]; b=a; b.append(4)` 后 `a` 是？  
4. 写递归 `fib(6)`，数一数 `fib(3)` 被算了几次。  
5. C++：`struct P{int x,y;};` 定义并访问。  
6. Py：`class Dog` 加 `bark`，两只狗各叫一声。  
7. `try: 1/0` 后程序还能否继续？  
8. 近一期三级真题：挑 2 道位运算 + 2 道数组阅读程序。
