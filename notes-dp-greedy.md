# DP 与贪心题单

配套：`?lesson=py4-3` `cpp6-1` `py6-3` `py6-2` `py6-6`  
VOX：`recur-vs-iter` `vox-maxsub` `vox-change` `vox-activity`

GESP 真题：https://gesp.ccf.org.cn/101/1010/index.html  
建议对照：四级爬楼梯/斐波那契、六级 DP/贪心编程题。

---

## 1. 爬楼梯递推

`f(n)=f(n-1)+f(n-2)`

题：  
1. 递归+递推各写 f(10)。  
2. 滚动两变量求 f(20)。  
3. 一次可跨 1/2/3 级，递推式？（f(n)=f(n-1)+f(n-2)+f(n-3)）

---

## 2. 最大子段和

`dp[i]=max(dp[i-1]+a[i], a[i])`，`best` 记最大。

```cpp
int dp = 0, best = -1e9;
for (int i = 0; i < n; i++) {
    dp = max(a[i], dp + a[i]);
    best = max(best, dp);
}
```

题：  
1. 跟算 `[-2,1,-3,4,-1,2,1,-5,4]`。  
2. 全负数组答案是什么。  
3. 为什么不能「直接全局 max」。

---

## 3. 贪心找零

面额从大到小，能拿几张拿几张。

题：87 元最少几张；若面额是 1,3,4，贪心还对吗？（反例 6：贪心 4+1+1=3 张，最优 3+3=2 张）

---

## 4. 活动选择

按**结束时间**排序，能接上就选。

题：  
1. 3 个活动手推。  
2. 若按开始时间排序会怎样（反例）。  
3. 证明「结束早」贪心正确性（课堂讨论）。

---

## 5. 易错

- DP 边界没写  
- 最大子段和初值写成 0（全负错）  
- 贪心排序键选错（开始时间）
