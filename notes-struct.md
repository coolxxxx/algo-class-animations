# C++ 结构体 · 课堂包

配套：`easy.html`（角色动画）+ 白板片。

---

## 1. 一句话概念

**结构体 = 把相关的多个变量，打包成一个自定义类型。**  
像学生档案袋：姓名、学号、分数钉在一起。

---

## 2. 核心语法（板书）

### 定义类型（图纸）

```cpp
struct Student {
    string name;
    int id;
    double score;
};  // 分号别丢！
```

### 使用（实物）

```cpp
Student stu;          // 创建变量
stu.name = "小明";     // 点号访问成员
stu.id = 1001;
stu.score = 92.5;
cout << stu.name << " " << stu.score;
```

### 结构体数组

```cpp
Student a[50];
for (int i = 0; i < n; i++) {
    cin >> a[i].name >> a[i].id >> a[i].score;
}
```

### 传参与排序

```cpp
void show(const Student& s) {
    cout << s.name << " " << s.score << endl;
}

bool cmp(Student x, Student y) {
    return x.score > y.score;  // 分数降序
}
sort(a, a + n, cmp);
```

---

## 3. 易错点

| 坑 | 正确做法 |
|---|---|
| 忘记 `struct` 后的分号 | `};` |
| 用 `stu.name` 时写成 `stu->name` | 普通变量用 `.`，指针才用 `->` |
| 类型名和变量名混用 | 类型 `Student`，变量 `stu` |
| 比较结构体整体 | 要自定义 `cmp` 比成员 |
| 传大结构体拷贝慢 | 函数参数用 `const Student&` |

---

## 4. 课堂流程（45–60 分钟）

1. **5′** 档案袋比喻（easy.html 第① tab）  
2. **12′** 定义→创建→赋值→访问（第② tab 单步）  
3. **10′** 黑板默写 `struct Student` + 三步使用  
4. **10′** 结构体数组 + 循环输入（第③ tab）  
5. **10′** 当堂题 1、2  
6. **5′** 总结：打包相关数据，点号访问  

---

## 5. 递进题单

1. **基础**：定义 `Point{x,y}`，读入两点，输出距离（可先只输出坐标和）。  
2. **基础**：定义 `Book{title, price}`，读入 n 本书，输出最贵的书名。  
3. **数组**：`Student a[n]`，按成绩降序输出姓名（自写 cmp 或选择排序）。  
4. **查找**：按学号 `id` 查找，输出该生信息；不存在输出 `-1`。  
5. **函数**：写 `double average(Student a[], int n)` 返回平均分。  
6. **综合**：班级成绩统计——输入 n 名学生，输出平均分、最高分、及格人数。

### 思考题

7. 结构体里能再嵌套一个结构体吗？（例如 `Student` 里有 `Point seat`）  
8. 什么时候该用结构体数组，什么时候该用多个平行数组？

---

## 6. 验收清单

- [ ] 能说出「为什么用结构体」（相关数据打包）
- [ ] 能默写定义 + 用 `.` 赋值/访问
- [ ] 能写结构体数组的循环输入
- [ ] 至少完成题 1、2
