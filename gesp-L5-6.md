# GESP 五～六级 · 考法讲义（可打印）

> 大纲 + 真题归纳，不转载整卷。

---

## 五级必会

| 知识点 | 2026-09 怎么考 | 注意 | 做题方法 |
|---|---|---|---|
| **链表** | 尾插复杂度；双向链表删结点代码；快慢指针找中点填空 | 先接线再 delete | 画 prev\|val\|next 三格 |
| **GCD** | `gcd(105,45)` 单选 | 纸笔 `%` 到 0 | 列 a,b,a%b 表 |
| **质数** | `isPrime` 循环边界最佳写法 | `i*i<=n` 溢出 | 想 n=2、n=4 |
| **排序** | 某轮后序列 / 比较交换次数 | 内层上界 | 只手推一轮 |
| **二分** | mid 与 lo/hi | 防溢出写法 | 三列表格 |
| **归并** | 合并结果 / 分治结构 | 稳定 | 双指针比最上张 |
| **贪心** | 找零、活动选择排序键 | **结束时间**不是开始 | 时间轴画绿条 |

---

## 六级必会

| 知识点 | 常见问法 | 注意 | 做题方法 |
|---|---|---|---|
| **栈/队列 STL** | 操作序列后 top/front | top 不弹出 | 竖画容器 |
| **括号匹配** | 是否匹配 / 栈内容 | 弹空前判空 | 遇右括号先看空 |
| **BFS 迷宫** | 最短步数；dist 含义 | 第一次到必最短；要标记 | 按层涂色 |
| **DFS / 全排列** | 排列个数；used 回溯 | 清 0 时机 | 画选择树 |
| **树遍历** | 先序/中序/后序序列 | 根在位置 | 树上描边 |
| **DP** | 爬楼梯、最大子段和、LCS 转移 | 边界 | 表格式跟算 |
| **类进阶** | 继承多态输出 | 虚函数 override | 画对象类型 |

---

## 展示台 / 本站资源

| 五级 | 六级 |
|---|---|
| 链表 `?lesson=cpp5-8` + VOX 链表 | 栈 `?lesson=cpp6-4` + VOX 栈 |
| GCD `?lesson=cpp5-11` + VOX GCD | 队列 `?lesson=cpp6-6` + VOX 队列 |
| 质数 `?lesson=cpp5-4` + VOX 筛 | BFS `?lesson=cpp6-7` + VOX BFS |
| 二分 `?lesson=cpp5-3` + 拼装器 | DFS `?lesson=cpp6-8` + VOX DFS |
| 归并 `?lesson=cpp5-6` + 拼装器 | 树 `?lesson=cpp6-9` + VOX 树 |
| 贪心活动 `?lesson=cpp5-7` | DP `?lesson=py6-3` + VOX 子段和 |

题单：`notes-sort.md` `notes-search.md` `notes-ds.md` `notes-dp-greedy.md` `notes-math.md`

---

## 课后自测 10 题

1. 无 head 哨兵，删除值为 x 的第一个结点，特判什么？  
2. `gcd(105,45)=?` 用递归式算。  
3. 判断 97 是否质数，循环到几？  
4. `[5,2,8,1]` 冒泡第一轮后？  
5. 有序数组找 7，第一次 mid 落哪？  
6. `(()())` 是否匹配？画栈。  
7. 6×6 迷宫手算最短一层层。  
8. n=3 全排列共几种，第 3 种是？  
9. 先序 A B D E C F G + 中序 D B E A F C G → 后序？  
10. `dp[i]=max(a[i], dp[i-1]+a[i])` 在最大子段和里什么意思？
