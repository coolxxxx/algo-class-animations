# 排序专题题单（冒泡 / 选择 / 插入）

配套：展示台 `?lesson=cpp5-1` `cpp5-2` `py5-2` · VOX `vox-bubble` / `vox-insert`

GESP 真题（官方 PDF）：https://gesp.ccf.org.cn/101/1010/index.html  
建议对照：三级/四级 Python·C++ 中「排序 / 比较次数 / 交换次数」选择题与阅读程序。

---

## 1. 必会概念

| 排序 | 核心动作 | 一轮结束 | 复杂度 |
|---|---|---|---|
| 冒泡 | 相邻比较，大的后换 | 最大冒到最后 | O(n²) |
| 选择 | 找最小与队首换 | 最小到最前 | O(n²) |
| 插入 | key 右移插入空位 | 前 i 个有序 | O(n²)（近有序更快） |

---

## 2. 骨架（C++）

```cpp
// 冒泡
for (int i = 0; i < n - 1; i++)
    for (int j = 0; j < n - 1 - i; j++)
        if (a[j] > a[j+1]) swap(a[j], a[j+1]);

// 选择
for (int i = 0; i < n - 1; i++) {
    int mi = i;
    for (int j = i + 1; j < n; j++)
        if (a[j] < a[mi]) mi = j;
    swap(a[i], a[mi]);
}

// 插入
for (int i = 1; i < n; i++) {
    int key = a[i], j = i - 1;
    while (j >= 0 && a[j] > key) { a[j+1] = a[j]; j--; }
    a[j+1] = key;
}
```

---

## 3. 题单

1. 手推 `[5,2,8,1,9]` 冒泡全程，记录每轮交换次数。  
2. 同一数组选择排序，比较交换次数与冒泡。  
3. 最坏情况 `[9,8,7,6,5]`：哪种交换更多？  
4. 最好情况 `[1,2,3,4,5]`：冒泡还能否优化（提前退出）？  
5. 插入排序：数组近有序时为什么快？  
6. 用插入排序排扑克牌，拍照/录屏对照代码。  
7. **GESP 对照**：最近一期三级选择题中涉及排序结果或比较次数的题，先做再对答案。  
8. 思考：为什么竞赛库 `sort` 比手写 O(n²) 快？（提示：快排/归并/堆）

### 易错

- 冒泡内层上界写成 `n` 而不是 `n-1-i`  
- 选择只比不换，忘了 `swap(a[i], a[mi])`  
- 插入 `while` 条件写成 `>` 还是 `>=`（稳定性）

---

## 4. 课堂 40′ 建议

VOX 5′ → 展示台逐步 10′ → 手推题 1、2 → 骨架默写 10′ → GESP 对照 10′
